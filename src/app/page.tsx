export default function HomePage() {
  return (
    <div className={"fixed h-screen w-screen bg-black text-white pt-16"}>
      Home page.
      <br/>
      Use the navbar to navigate around<br/><br/><br/><br/><br/>
      Normalize - Use GPT-4t to normalize a text using a clever system prompt. Also gives a comparison of the generated embeddings for each text.

    </div>
  );
}
